[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/api/financeiro/config"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[volumes]]
mountPath = "/app/data"
